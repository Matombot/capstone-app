<h1>patients info</h1>
<table class="table">
    {{#each patients}}

    <tr>
        <td>
            <a href="/appointment/{{id_number}}">{{first_name}}</a></td>
        <td>{{last_name}}</td>
        <td>{{id_number}}</td>
        <td>{{contact_no}}</td>

    </tr>
    {{/each}}
</table>
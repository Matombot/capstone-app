<h1>patients info</h1>
<table class="table">
    {{#each patients}}

    <tr>
        <td>{{first_name}}</td>
        <td>{{last_name}}</td>
        <td>{{id_number}}</td>
        <td>{{contact_no}}</td>

    </tr>
    {{/each}}
</table>